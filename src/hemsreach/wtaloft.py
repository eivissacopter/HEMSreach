def get_wind_at_altitude(location):
    # Function to get wind data at altitude
    # Assume a function to fetch the nearest weather data based on location
    # Replace this with actual implementation to get the wind data
    wind_data = {
        'wind_direction': 180,
        'wind_speed': 15,
        'freezing_level': 10000
    }
    return wind_data
